AI_BASE_URL=https://api.deepseek.com/v1
AI_MODEL=deepseek-flash
AI_API_KEY=replace-with-your-key
PORT=4317
CODE_SERVER_PORT=4318
# EDITOR_ENABLED=1 is set automatically by `npm run launch` when the editor runs.
# Leave it unset for Agents-only mode (e.g. Windows without WSL).
# WORKSPACE_ROOT=/absolute/path/to/a/project
